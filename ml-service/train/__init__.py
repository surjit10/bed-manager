"""
Training scripts for ML models
Each model has its own training script that:
1. Connects to MongoDB
2. Extracts and preprocesses data
3. Trains the model
4. Saves the trained model to /models directory
"""
